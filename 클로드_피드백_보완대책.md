# 🛠️ Claude 피드백 기반 기하학적 물리 분석 보완 대책

이 문서는 Claude의 예리한 피드백(데이터 증강 편향, 데이터 클리닝 위험성, Windows VRAM 단편화, 피사체 추적 한계)을 해결하기 위한 **구체적인 구현 및 보완 설계안**입니다. 이 내용을 팀원(정현이 형)과 공유하여 학습 파이프라인에 즉시 반영할 수 있습니다.

---

## 📌 1. 데이터 증강 (Augmentation) 보완책

### A. 캡션 방향성 자동 스왑 (Caption Direction Swapping)
* **문제**: 이미지를 좌우 반전(Horizontal Flip)할 때 캡션의 이동 묘사(`left` ↔ `right`)를 수정하지 않으면 모순 데이터가 발생합니다.
* **보완안**: 데이터 로더(Dataloader) 또는 증강 스크립트 단에서 정규식을 이용해 캡션을 매핑합니다.
  ```python
  def swap_caption_direction(sentence):
      # 영문 및 국문 키워드 방향 교차 매핑
      mapping = {
          r"\bleft\b": "right", r"\bright\b": "left",
          r"\bleftward\b": "rightward", r"\brightward\b": "leftward",
          r"\bleftwards\b": "rightwards", r"\brightwards\b": "leftwards",
          "왼쪽": "오른쪽", "오른쪽": "왼쪽",
          "좌측": "우측", "우측": "좌측"
      }
      # 임시 플레이스홀더를 사용하여 교차 스왑 시 덮어쓰기 방지
      s = sentence.lower()
      # 예: left -> temp_right, right -> temp_left
      for k, v in mapping.items():
          s = re.sub(k, f"temp_{v}", s)
      for k, v in mapping.items():
          s = s.replace(f"temp_{v}", v)
      return s
  ```

### B. 쉬운 패턴 과적합 방지: 커리큘럼 학습 (Curriculum Learning)
* **보완안**: 물리 법칙이 100% 명확히 들어맞는 정합 샘플만 편식 학습하면 실제 거친 데이터(노이즈)에 취약해집니다.
  * **1단계 (Epoch 1~2)**: 물리 법칙 정합률이 높은 샘플(Clean subset) 위주로 학습하여 비디오의 순차적 흐름과 시간 관계의 기초를 잡습니다.
  * **2단계 (Epoch 3~5)**: 전체 데이터셋(노이즈 및 복잡한 물리 유형 포함)을 투입하여 실전 비디오 도메인의 복잡성에 적응시킵니다.

---

## 📌 2. 데이터 클리닝 및 노이즈 필터링 보완책

### A. 단순 탐지 실패와 실제 물리 법칙 위반의 분리
* **보완안**: 40%의 불일치 데이터를 일괄 삭제하지 않고, **탐지 실패**와 **진짜 모순**을 엄격히 나눕니다.
  1. **판정 불가 (Keep)**: 4개 프레임 중 객체가 2개 미만으로 검출되어 트래킹이 안 된 경우 $\rightarrow$ 이 데이터는 정상 학습용으로 **그대로 유지**합니다.
  2. **진짜 모순 (Soft-Weighting)**: 4개 프레임 모두 객체가 안정적으로 탐지되었으나 X좌표나 면적 흐름이 캡션과 정반대로 나오는 경우 $\rightarrow$ 이 샘플들은 학습에서 완전히 제거하는 대신 **Loss 가중치를 0.3~0.5배로 낮추어(Soft-down)** 잘못된 레이블 노이즈가 모델에 미치는 악영향을 억제합니다.

---

## 📌 3. Windows CUDA OOM 단편화 보완책

### A. PyTorch Allocator 환경변수 주입
* **보완안**: 학습 스크립트 실행 환경에 메모리 확장을 유연하게 만들어주는 설정을 강제 적용합니다.
  ```powershell
  # PowerShell 실행 스크립트 상단 추가
  $env:PYTORCH_CUDA_ALLOC_CONF="expandable_segments:True"
  ```
  *(메모리 단편화 현상을 방지해 540스텝 병목 구간을 근본적으로 극복할 수 있습니다.)*

### B. 다이나믹 해상도 버킷팅 (Dynamic Resolution Bucketing)
* **보완안**: Qwen2-VL 입력 이미지의 무작위 해상도로 인한 메모리 파편화를 막기 위해 `torch.backends.cudnn.benchmark = False`를 설정하여 고정 탐색을 방지하고, 가능한 경우 해상도의 종횡비(Aspect Ratio)를 3~4개의 대표 버킷으로 통일하여 배치(Batch)를 구성합니다.

---

## 📌 4. 피사체 추적 한계 보완책 (Identity Tracking)

### A. CLIP Crop 거리 필터링 및 통계적 안전성 검증
* **실측 검증 결과 (`scratch/calculate_crop_clip_distances.py` 수행)**:
  * holdout 데이터셋 샘플 대상 실측 결과, 동일 피사체(Same Subject) 크롭 간의 평균 CLIP 거리는 **0.2140** (표준편차 $\sigma=0.0517$, 중앙값: 0.2132, 관측 최댓값: 0.2863)으로 집계되었습니다.
  * 다른 비디오의 피사체(Different Subject) 간의 평균 CLIP 거리는 **0.3374** (중앙값: 0.3292)로 집계되었습니다.
  * **지적 사항 입증**: 만약 전체 장면 전환용 임계값인 **`0.20`을 크롭 이미지 비교에 그대로 사용하면, 정상적인 동일 피사체 추적 데이터의 70.8%를 "추적 실패(유실)"로 오판정(False Alarm)**하여 멀쩡한 단서를 차단하는 논리적 모순이 발생합니다.
* **⚠️ 표본 크기 제한성 및 통계적 보완책 (순환 검증 리스크 제어)**:
  * **극단값 통계(Extreme Value Statistics)의 한계**: 현재 실측된 Same-Subject 쌍의 표본 크기($N=24$)가 작아, 관측된 최댓값(`0.2863`)을 동일 인물 모션 변화의 절대적 물리적 한계로 단정하기 어렵습니다. 표본이 늘어날수록 극단적인 포즈/가림으로 인해 실제 최댓값은 더 치솟을 수 있습니다.
  * **보수적 통계 버퍼 적용**: 표본 부족으로 인한 한계를 극복하기 위해 단순히 관측 최댓값을 신뢰하지 않고, **3-Sigma Rule (Mean + 3 $\times$ Std = 0.2140 + 0.1551 = 0.3691)** 또는 **관측 최댓값 대비 10%의 여유 버퍼(Max $\times$ 1.1 = 0.3149)**를 고려합니다.
  * **잠정 임계값(Provisional Threshold) 설정**: 이에 따라 정상 데이터의 오판율을 최소화하기 위해 **`0.30`을 잠정적인 안전 임계값**으로 채택(오판정 70.8%인 0.20 대비 압독적 개선)하여 적용하되, 향후 표본 크기를 $N \ge 100$ 이상으로 확장하여 3-Sigma 구간을 재확인한 후 최종 확정하는 단계를 제안합니다.
* **보완안**: 4장의 이미지에서 OWL-ViT가 검출한 BBox 영역을 잘라내어(Crop) CLIP 거리(Distance = 1 - Cosine Similarity)를 구하되, 임계값은 장면 전환(0.20)과 분리하여 잠정적으로 **`0.30`**으로 적용하여 마스킹 처리합니다.
  1. OWL-ViT가 검출한 각 BBox 영역을 크롭하여 CLIP 임베딩을 추출합니다.
  2. 인접 프레임 간 크롭 이미지의 CLIP 거리를 계산합니다.
  3. 만약 인접 프레임 간 물체 거리가 임계값 **0.30을 초과**하는 구간이 있다면, "동일 장면 내부에서 Tracker가 다른 사물로 미끄러졌다(Identity Jump)"고 판단하여 해당 궤적 힌트를 마스킹 처리합니다.
