# Kaggle 8B 학습 — 완전 초보용 사용법

> 목표: 로컬(8GB)에서 못 돌리는 **8B 모델**을 Kaggle 무료 GPU로 학습 → 점수 도약 도전.
> 필요한 파일: `KAGGLE_ALL_IN_ONE.py` (이거 하나면 학습+제출 다 됨) + `aux_upload/` 안 CSV 3개.

---

## 📋 준비물 체크
- [ ] Kaggle 계정 (GPU 쓰려면 **전화번호 인증** 필요 — Settings → Phone Verification)
- [ ] 이 폴더의 `KAGGLE_ALL_IN_ONE.py`
- [ ] 이 폴더의 `aux_upload/` 안 CSV 3개

---

## 1단계: 보조 데이터 올리기 (5분, 한 번만)

1. Kaggle 접속 → 왼쪽 메뉴 **Datasets** → **+ New Dataset**
2. `aux_upload/` 폴더 안의 **3개 파일**을 끌어다 놓기:
   - `aug_weights_exp16.csv`
   - `snu_clip_features.csv`
   - `holdout_300.csv`
3. Dataset 이름: 아무거나 (예: `snu-ai-aux`) → **Create**

> 💡 이 파일들은 "어떤 데이터를 얼마나 학습할지" 정하는 우리 노하우입니다. 없어도 돌아가지만 성능이 떨어집니다.

---

## 2단계: 노트북 만들고 코드 붙이기 (5분)

1. Kaggle → 왼쪽 **Code** → **+ New Notebook**
2. 오른쪽 설정 패널에서:
   - **Accelerator** → **GPU T4 x2** 선택 (제일 중요!)
   - **Internet** → **On** (모델 다운로드에 필요)
3. 오른쪽 **Add Input** 버튼 → 두 개 추가:
   - 대회 데이터 (SNU AI Challenge 데이터셋 검색해서 추가)
   - 방금 만든 `snu-ai-aux`
4. 노트북 첫 셀에 **`KAGGLE_ALL_IN_ONE.py` 내용을 통째로 복사 → 붙여넣기**
5. **▶ 실행** (Run) — 또는 상단 **Run All**

> 데이터 경로는 코드가 **자동으로 찾습니다**. 수정할 것 없습니다.
> 실행하면 "✅ 대회 데이터: ..." 가 뜨는지 확인하세요. 안 뜨면 3번(Add Input)을 다시 하세요.

---

## 3단계: 학습 기다리기 (반나절~하루)

- 학습이 시작되면 진행바가 움직입니다. **8B라 느립니다 (10시간 이상 가능).**
- Kaggle은 **한 세션이 최대 12시간**입니다. 12시간에 끊기면:

### ⚠️ 세션이 끊겼을 때 이어하기 (매우 중요 — 자동화됨)

Kaggle은 12시간 후 노트북을 멈추고 `/kaggle/working`을 지웁니다.
**하지만 코드에 "세션 간 자동 재개"가 들어있어서, 아래 순서만 지키면 저절로 이어집니다.**

#### 학습을 돌릴 때는 반드시 "Commit"으로
1. 그냥 Run(▶) 말고, 상단 **Save Version → Save & Run All (Commit)** 클릭
2. Kaggle이 백그라운드에서 최대 12시간 돌리고 **결과(체크포인트 포함)를 그 버전에 영구 저장**합니다
3. 다 돌면 로그 맨 아래 확인:
   - `종료: 850/1488 스텝 (57%)` → **미완주**, 이어하기 필요 (아래 4번)
   - `✅ 완주!` → 끝. 4단계(제출)로

#### 이어하기 (미완주일 때) — 이게 "밖으로 저장한 모델 불러오기"
4. 방금 Commit한 노트북에서 **Copy & Edit** (또는 같은 노트북 새 버전)
5. 오른쪽 **Add Input** → **Notebook Output** 탭 → **방금 그 노트북의 출력**을 추가
   - (이러면 이전 체크포인트가 `/kaggle/input/...`에 붙습니다)
6. 다시 **Save & Run All (Commit)** → 코드가 이전 체크포인트를 **자동으로 찾아 복사**하고
   `♻️ 이전 세션 체크포인트 복원: ... (step 850)` 을 출력하며 **이어서 학습**합니다
7. `✅ 완주!` 나올 때까지 4~6번 반복 (보통 8B는 2세션이면 완주)

> 💡 요약: **① Commit으로 돌린다 → ② 미완주면 그 Output을 Input에 넣고 다시 Commit → 반복.**
> 코드가 알아서 이어받으니, 당신은 "이전 출력을 Input에 추가"만 하면 됩니다.

---

## 4단계: 제출 파일 만들기 (1~2시간)

학습이 **✅ 완주!** 되면:

1. 코드 맨 아래쪽에서 이 줄을 찾으세요:
   ```python
   RUN_INFERENCE = False   # ← 학습 완주 후 True로 바꾸고...
   ```
2. `False` → **`True`** 로 바꾸기
3. 다시 실행 → test 819개를 추론해서 `submission.csv`를 만듭니다
4. 오른쪽 **Output** 탭 → `submission.csv` **다운로드**
5. 대회 페이지에 **제출!**

---

## ❓ 자주 나는 문제

| 증상 | 해결 |
|---|---|
| "대회 데이터를 못 찾음" | Add Input에서 대회 데이터셋 추가했는지 확인 |
| 설치가 오래 걸림 | 정상입니다 (2~3분). 인터넷 On 확인 |
| OOM (메모리 부족) 에러 | 코드가 자동으로 그 샘플만 건너뜁니다. "OOM스킵 N" 나오면 정상 |
| 12시간에 끊김 | 3단계 "방법 A" 참고 — 다시 실행하면 이어감 |
| 진행률이 안 오름 | 8B는 원래 느립니다. 로그 시간(h)이 늘면 정상 |

---

## 🎯 이 학습에 뭐가 들어갔나 (성능 근거)

**넣은 것 (실측으로 점수 오른 것만):**
- 8B 모델 (로컬 4B보다 큰 체급 — 점수 도약 기대)
- 문장을 뒤에 놓는 프롬프트 (v5) — 실측 +0.9%p
- 약점 유형 집중 증강 — 실측 최고 레시피
- 헷갈리는 프레임 섞기 (어려운 셔플) — 주 오답 공략
- 추론 시 24개 답 전부 채점 (우도 스코어링) — 로컬 검증됨

**뺀 것 (넣으면 점수 떨어지는 것):**
- CoT, 이미지 힌트 주입, gemma 힌트 — 전부 실측에서 실패해서 제외

즉 **"검증된 것만 8B에 최대로"** 담았습니다.

---

## 🔗 로컬과의 관계
- 로컬에서는 4B(exp17, Public 0.857)가 계속 돌고 있습니다 → **안전 폴백**
- Kaggle 8B가 0.857을 넘으면 → 그걸 최종 제출
- Kaggle이 실패해도 로컬 4B가 있으니 **잃을 것 없습니다**
