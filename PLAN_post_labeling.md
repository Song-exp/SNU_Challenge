# 계획 — gemma 라벨 완성 이후 (7/17 오후 수립)

> 전제: train 전량 라벨링 진행 중 (~8.9초/문장, ETA 7/18 오전 09시대), 마감 7/24.
> 근거 실측: camera +27%p / n_events 계단식 / exp12 손실은 분해 품질이 주범(깨끗한 분해는 동률) / 무학습 힌트 주입 무효.

## 트랙 구조

- **트랙 1 (본선): 타깃 증강** — **holdout 약점 분석**으로 증강 가중 (⚠️ test 분포를 학습 설계에 쓰면 실격 — §규정). 검증된 축(camera, n_events)부터. exp15가 1호.
- **트랙 2 (게이트制): 구조 정보 활용 학습** — 같은 gemma 라벨을 쓰는 두 방식의 미니 대결:
  - A. `mini_hint`: 라벨을 **입력 힌트**로 프롬프트에 주입 (오류 전파 없음, 추론 쌈, 단 test 라벨 필요 → 규정 확인 선행)
  - B. `mini_gemma_cot`: gemma events를 **CoT 타깃**으로 SFT (자가 생성이라 규정 안전, 추론 13배 비쌈)
  - 기준선 mini_v1_aug2 = 15.5%. **+4%p 이상 이긴 쪽만 본학습 확전** (exp14의 미니-full 괴리 전례 준수)
- **트랙 3 (보류 카드)**: CLIP 전환점 힌트(트랙 2 승자에 합류 가능, 규정 확인 필수), 4B 스케일업, exp08(균일 aug4).

## 타임라인 (슬롯 단위)

| 시점 | GPU | CPU 병행 |
|---|---|---|
| **7/17 오후~밤** | 라벨링 계속 (train 전량) | ~~① exp15 구현 ② 규정 확인~~ (완료 — `Structure_Pipeline.ipynb`·`structure_features.py`·`--aug-weights` 구축, 규정 판정은 §규정) ③ v6_hint 프롬프트·CoT 타깃 포맷 설계 ④ git 커밋 |
| **7/18 오전** | 라벨링 완료(~09시) → exp15 스모크 → **exp15 본학습 시작** (~20h, 낮 스로틀 감안) | 라벨 품질 점검(①셀), holdout 약점 기준선 확정(②셀). test 819 라벨링은 A트랙이 미니 게이트를 통과했을 때 **추론 전처리 용도로만** |
| **7/19 오전~낮** | exp15 완료 → 평가 → **판정 vs exp07 48.41%** | 이기면 제출 1회로 검증 |
| **7/19 낮~저녁** | **미니 체인**: mini_hint → mini_gemma_cot (+ 여유 시 4B 미니) 각 ~1.5h+평가 | 4B go/no-go 결정 시한 |
| **7/19 밤** | 밤 슬롯: 미니 승자 본학습 or 4B or exp08 | |
| **7/20~23** | 본학습 슬롯 2~3회 + 제출 검증 | 재현성 패키징 준비 |
| **7/24** | 최종 제출 | |

## 판정 게이트 (미리 합의)

1. **exp15 vs exp07 48.41%**: +2%p↑ = 주력 교체·제출 검증 / 이내 = exp07 유지, 트랙 1은 n_events 축으로 1회 더 / 하락 = 타깃 증강 기각
2. **미니 2종 vs 15.5%**: +4%p↑만 확전. A가 이기면 규정 확인 결과가 확전의 전제조건
3. **4B**: 7/19-20까지 미니 스크리닝 못 태우면 자동 폐기 (완주 불가)
4. 라벨링 ETA가 크게 밀리면(낮 스로틀) 미니 풀 완성 시점에 끊고 exp15 먼저 — 이어받기 안전하므로 나머지는 유휴 슬롯에 재개

## 규정 (7/17 `PROJECT_SETUP.md` §4.3 정독 결과)

- **"평가 데이터 특성 분석 후 학습" = 명시적 실격 사유** → 증강 가중·실험 우선순위의 근거는 holdout/train 실측만. test 분포는 학습 설계의 어떤 결정에도 사용·인용하지 않음
- **추론 시 전처리(gemma 라벨·CLIP 피처)는 허용 판단** — 앙상블 금지는 "추론 결과 조합"으로 정의됨, 피처/라벨은 답이 아님. 단 gemma4 가중치 공개일(2026-05-31 이전) 확인은 A트랙 확전 전 필수
- 어댑터 2개 라우팅은 여전히 불가 (추론 결과 조합)

## 리스크
- **미니-full 괴리**: exp14에서 미니 +6.4%p가 full에서 소멸 — 미니 승리는 확전 근거일 뿐 제출 근거 아님
- **VRAM**: 4B는 4bit 필수 + 스모크에서 peak 확인 (7.5GB 초과 시 중단)
- 제출은 팀 전체 1일 2회 — 검증은 holdout, 제출은 판정 승자만
